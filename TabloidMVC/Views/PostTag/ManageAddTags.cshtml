@model TabloidMVC.Models.ViewModels.PostTagViewModel

@{
    ViewData["Title"] = "ManageAddTags";
}

<h1>Manage Adding Tag</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                <span>Tag Name</span>
            </th>
            <th>
                <span>Add Tag Relationship</span>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var tag in Model.TagOptions)
        {
            <tr>
                <td>
                    <span>@tag.Name</span>
                </td>
                <td>
                    <form asp-action="ManageAddTags" asp-route-postId="@Model.Post.Id" asp-route-tagId="@tag.Id">
                        <input type="submit" value="Add" class="btn btn-danger" />
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-controller="Post" asp-action="Details" asp-route-id="@Model.Post.Id" >Back to Post Details</a>